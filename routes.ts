export const PUBLIC_ROUTES = ["/"];

export const AUTH_ROUTES = ["/signin", "/signup", "/forget-password"];

export const DEFAULT_ROUTE = "/";

export const LOGIN_ROUTE = "/store";

export const UPLOADTHING_PREFIX = "/api/uploadthing";

export const ACCOUNT_VERIFICATION_PREFIX = "/account-verification";

export const STRIPE_WEBHOOK_ROUTE = "/api/stripe/webhook";

export const API_ROUTE_PREFIX = "/api/auth";

export const CANVAS_ROUTE = "/canvas";
